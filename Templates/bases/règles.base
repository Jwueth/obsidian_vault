views:
  - type: table
    name: Table
    filters:
      and:
        - Catégories.contains(link("Catégorie/Règles", "Règles"))
    order:
      - file.name
    sort:
      - property: file.tags
        direction: ASC
